const cart=[];const money=n=>new Intl.NumberFormat('ru-RU').format(n)+' ₸';const panel=document.querySelector('#cartPanel'),overlay=document.querySelector('#overlay');
function render(){const items=document.querySelector('#cartItems'),total=cart.reduce((sum,item)=>sum+item.price,0);document.querySelector('#cartCount').textContent=cart.length;document.querySelector('#cartTotal').textContent=money(total);items.innerHTML=cart.length?cart.map((item,i)=>`<div class="cart-item"><div><strong>${item.name}</strong><br><span>${money(item.price)}</span></div><button aria-label="Удалить" data-remove="${i}">×</button></div>`).join(''):'<p class="empty">Здесь пока пусто.<br>Выберите обои из коллекции.</p>';document.querySelectorAll('[data-remove]').forEach(btn=>btn.onclick=()=>{cart.splice(btn.dataset.remove,1);render()})}
function toggle(open){panel.classList.toggle('open',open);overlay.classList.toggle('show',open);panel.setAttribute('aria-hidden',!open)}
document.querySelectorAll('.quick-add').forEach(btn=>btn.onclick=()=>{cart.push({name:btn.dataset.name,price:+btn.dataset.price});render();toggle(true)});document.querySelector('#cartButton').onclick=()=>toggle(true);document.querySelector('#closeCart').onclick=()=>toggle(false);overlay.onclick=()=>toggle(false);
document.querySelectorAll('.filter').forEach(btn=>btn.onclick=()=>{document.querySelectorAll('.filter').forEach(x=>x.classList.remove('active'));btn.classList.add('active');document.querySelectorAll('.product').forEach(p=>p.style.display=btn.dataset.filter==='all'||p.dataset.kind===btn.dataset.filter?'block':'none')});
document.querySelector('#calculator').onsubmit=e=>{e.preventDefault();const f=new FormData(e.target),area=+f.get('width')*+f.get('height'),rolls=Math.ceil(area/4.5);document.querySelector('#calcResult').textContent=`Вам понадобится примерно ${rolls} ${rolls===1?'рулон':'рулонов'} (с запасом 10%).`};
